# 文档审阅

当用户要求分析 Office 或 PDF 文档时：

1. 使用 `read_document` 读取，不要把二进制文档交给 `read_file`。
2. 若结果包含 `truncated: true`，使用 `nextOffset` 继续读取，直到覆盖与目标相关的全部章节。
3. 区分文档原文、公式缓存值和你的推断；重要结论注明对应章节、工作表或幻灯片。
4. 遇到扫描 PDF 无文本层、加密文件或旧 DOC/PPT 时，明确说明当前解析边界，不要猜测缺失内容。
5. 生成摘要前先核对数字、日期、名称和结论在不同章节中是否一致。
