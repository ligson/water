# AGENTS.md

本仓库不提交个人机器上的私有 Agent 配置路径；本地开发者可在自己的 Codex/Agent 环境中按需加载额外规则。

## 项目概述

**若水（Water）** 是一个**全自托管、内网/离线可用**的 AI 编程智能体（Coding Agent）。

核心理念来自《道德经》「上善若水」与 Harness 工程思想：不是让模型自由发挥，而是通过精心设计的脚手架（Harness），**以柔驭强**，把大模型稳定、安全、可控地驾驭起来。

目标对标 OpenAI Codex / Claude Code 的核心能力，但完全运行在用户自己的服务器与内网环境中。

一句话定位：**可驾驭的私有 AI 编程助手：本地大模型 + 强约束工具 + 网页交互。**

## 技术栈

| 层级 | 技术选型 | 说明 |
| --- | --- | --- |
| 后端 | **Golang** | 主语言，负责 Agent 循环、工具执行、权限、API |
| 前端 | **Vue 3 + TypeScript + Vite + Ant Design Vue** | 网页交互界面 |
| 大模型 | 本地部署或内网模型服务 | 统一通过 OpenAI-compatible 接口调用 |
| 数据存储 | **SQLite** | 单用户、本地优先，存储工作区、任务、消息、事件、审批与配置 |
| 通信 | HTTP + WebSocket | HTTP 用于普通 API，WebSocket 用于任务流、工具事件、审批和打断 |
| 脚本执行 | 离线 Python 环境 | Agent 生成的脚本优先用预置本地/容器 Python 运行 |

禁止随意引入 Node.js 后端、Python 主服务或其他会破坏「Go 后端 + Vue 前端」边界的技术。

## 目录约定

项目采用前后端分离目录：

```text
water/
├── water-be/              # Go 后端
├── water-fe/              # Vue 3 + TypeScript + Vite + Ant Design Vue 前端
├── docs/                  # 中文优先的设计、架构、阶段规划文档
├── CHANGELOG.md           # 每次仓库改动必须记录
├── AGENTS.md              # Agent 工作规则
└── README.md
```

后端推荐细分方向：

```text
water-be/
├── cmd/water/
├── internal/
│   ├── agent/             # Agent 循环、状态机、上下文压缩
│   ├── api/               # HTTP / WebSocket 接口
│   ├── auth/              # 单用户本地权限模型
│   ├── config/            # 配置加载、页面配置持久化
│   ├── llm/               # OpenAI-compatible provider 抽象
│   ├── sandbox/           # 命令、Python、文件访问隔离
│   ├── store/             # SQLite 数据访问
│   └── tools/             # 工具定义、参数校验、执行与审计
├── configs/
├── migrations/
└── go.mod
```

## 核心设计原则

1. **Harness 优先，而不是模型优先**
   模型只是引擎。稳定性、安全性、可维护性来自工具设计、权限控制、循环约束、审计和可干预机制。

2. **默认安全，显式放权**
   文件操作默认限制在工作区目录内。命令执行、文件写入、网络访问、Python 运行必须经过权限策略判断，并保留人工审批扩展点。

3. **完全离线/内网可用**
   不依赖外部公网 API。模型、Python 环境、依赖、数据库和配置都按可离线运行设计。除非用户明确配置，不假设可以访问公网。

4. **可观测与可干预**
   Agent 的每一步工具调用、审批、命令输出、文件变更和模型流式输出都要能展示、打断、回放和审计。

5. **简单胜过复杂**
   先实现最小可用 Agent Loop + 核心工具 + 流式 UI，再逐步增加 Skills、Sub-Agent、长期记忆、并行等高级能力。

## 已定设计决策

- 后端目录使用 `water-be/`，前端目录使用 `water-fe/`。
- 后端使用 Go + SQLite。
- 前端使用 Vue 3 + TypeScript + Vite + Ant Design Vue。
- LLM 先只实现 OpenAI-compatible 调用，但 Provider 必须可配置、可切换，不允许写死 base URL、model 或 API key。
- Provider 配置优先通过页面完成，保持简单：名称、接口地址、模型、API Key、是否默认、连接测试；高级参数折叠隐藏。
- Provider API Key 第一版直接存 SQLite，不做加密；页面脱敏展示，日志不得输出明文。
- HTTP JSON 接口必须使用统一响应 envelope：`success`、`requestId`、`message`、`httpCode`、`data`。
- Water 是单用户应用，不做复杂多用户租户系统。
- Water 必须支持多个工作区。工作区类似 Codex 的项目上下文，是任务、默认 Provider 和权限模式的主要归属。
- 工作区是默认执行上下文，但用户可以在任务或审批中授权访问外部文件路径；外部路径授权按工作区持久保存，并应支持查看和撤销。
- 前端与后端之间优先使用 WebSocket 承载任务事件流、审批、打断和工具进度。
- 后端与 OpenAI-compatible 模型服务之间按 provider 协议消费 HTTP/SSE 流。
- 权限模式第一版只保留两个用户可理解的选项：`请求审批` 与 `完全访问`。

更多设计细节维护在 `docs/` 目录。

## 权限模式约定

### 请求审批

默认推荐模式。

- 只读工具（如列目录、读文件）可在工作区内自动执行。
- 写文件、删除文件、命令执行、Python 执行、网络访问、跨工作区或外部路径访问默认需要审批或策略允许。
- 审批请求必须包含：动作类型、目标路径或命令、风险说明、预期影响、可取消入口。

### 完全访问

高信任模式。

- 在当前工作区内，文件写入、命令执行、Python 执行可不逐项请求审批。
- 仍需记录审计事件。
- 是否允许访问工作区外路径由用户在任务或审批中明确授权，不应被“完全访问”隐式打开。
- UI 必须明确展示当前处于高信任模式。

### 写文件预览

- 第一版不做复杂 Diff 编辑器。
- 写文件审批必须至少展示目标路径、变更摘要，以及可用时的简单文本 diff 或前后片段。

## HTTP 响应规范

所有对外 JSON 接口必须使用统一 envelope：

```json
{
  "success": true,
  "requestId": "xxx",
  "message": "",
  "httpCode": 200,
  "data": {}
}
```

- `success` 表示接口处理是否成功。
- `requestId` 表示每次请求的唯一 ID。
- `message` 表示给前端显示的说明信息。
- `httpCode` 必须与 HTTP 状态码保持一致。
- `data` 表示业务数据主体，没有数据时返回空对象。
- 新增接口时优先复用统一响应封装，避免每个 handler 手写不同格式。

## 编码规范

### Go

- 使用标准 Go project layout。
- 错误处理要完整，禁止忽略 error。
- 工具执行相关代码必须考虑超时、取消（context）和资源清理。
- 对外暴露的接口要有清晰注释。
- Provider、Tool、Approval、Workspace 等核心边界要优先设计接口，再写实现。

### Vue

- 使用 Vue 3 + Composition API + TypeScript。
- 使用 Ant Design Vue 作为基础组件库。
- 组件尽量小而清晰。
- 流式对话、工具事件、简单变更预览、操作审批、工作区切换是前端核心体验。
- 用户面向文案优先中文。

### 通用

- 配置与代码分离。
- 敏感信息不得硬编码。
- 优先写可测试的代码。
- 文档优先中文，代码与代码注释使用英文。

## Agent 行为指南

当你在本仓库中工作时，请遵循以下规则：

1. **先理解再动手**
   修改前先阅读相关文件，确认当前架构意图。

2. **先讨论设计，再初始化骨架**
   除非用户明确要求，不要直接创建完整项目骨架或大规模代码。

3. **安全第一**
   任何涉及命令执行、文件写入、Python 运行的功能，必须默认保守，并留下人工确认、白名单或权限模式机制。

4. **保持技术栈纯净**
   后端只写 Go，前端只写 Vue/TypeScript。不要引入其他后端语言。

5. **最小可行变更**
   一次只解决一个明确问题，避免大规模重构，除非用户明确要求。

6. **文档同步**
   新增重要模块、改变核心流程、确定设计决策时，同步更新 `docs/` 或 README。

7. **必须维护 CHANGELOG**
   每次对仓库做任何文件改动，都必须更新根目录 `CHANGELOG.md`，记录日期、改动内容和影响范围。

8. **提交前禁止携带本地敏感信息**
   提交或推送前必须检查 `git status`、暂存区 diff 和新增文件，禁止提交 API Key、Token、Cookie、私钥、`.env*`、本地数据库、日志、PID、Playwright 会话、构建产物、绝对私有路径或其他只属于本机环境的信息。发现敏感内容时应先移除、脱敏或加入 `.gitignore`，再提交。

9. **本地模型优先**
   所有 LLM 调用都走可配置的 OpenAI-compatible 接口，方便对接 Ollama / vLLM / LM Studio / 内网模型网关。

10. **明确的发版流程**
    用户明确说“发版”时，先完成测试、敏感信息审计、CHANGELOG 更新、提交和推送，再创建并推送 `vMAJOR.MINOR.PATCH` annotated tag 触发 GitHub Actions。用户未指定版本时，在最新 SemVer 标签基础上递增 patch；仓库没有标签时从 `v0.1.0` 开始。不得复用或强制移动已发布标签。

## 当前阶段建议

优先实现顺序：

1. 设计文档与关键边界确认。
2. 最小 Go HTTP 服务 + SQLite 配置存储。
3. OpenAI-compatible Provider 管理与连接测试。
4. 基础 Chat API 与模型流式输出。
5. WebSocket 任务事件流。
6. 基础工具：列目录、读文件、写文件。
7. Agent Loop：模型输出工具意图，Harness 校验后执行。
8. 权限模式与审批 UI。
9. Python 脚本隔离执行。
10. 再考虑记忆系统、多轮规划、Sub-Agent 等。

## 禁止事项

- 不要假设有公网访问。
- 不要把高危命令默认设为自动执行。
- 不要引入重型框架导致部署复杂化。
- 不要破坏 Go 后端 + Vue 前端的清晰边界。
- 不要生成与项目无关的示例代码或过度设计。
- 不要在用户未确认前初始化完整项目骨架。

## 项目精神

上善若水。  
水善利万物而不争，处众人之所恶，故几于道。

我们要做的不是最喧闹的 AI，而是最稳、最可控、最懂得「何时进、何时止」的编程伙伴。
